# 운영설정과 테스트환경 설정은 따로 가는 것이 좋음
# inmemoryDB mode로 테스트
spring:

#spring:
#  datasource:
#    url:  jdbc:h2:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop (테스트 환경에서 기본)
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug